# 斯波索宾和声分析器

一个可部署到 Vercel 的静态网站 Demo。它根据输入旋律和调号运行一段和声分析流水线，展示调性、音符类型与功能和声候选。

## 本地预览

直接打开 `index.html` 即可预览。

## 部署

这个项目不需要安装依赖。把整个文件夹推送到 GitHub 后，在 Vercel 中导入仓库，Framework Preset 选择 `Other`，输出目录保持默认即可。
